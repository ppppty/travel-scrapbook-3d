---
name: travel-scrapbook-3d
description: 将旅行照片和行程制作或改进成可交互的 3D 手账网站，支持照片拼贴、立体地标、翻页、封面开合及网站演示视频。适用于旅行照片做成网站、3D 旅行日记、莫奈风手账、给手账网站录展示视频等请求；不用于普通游记文案、行程规划或仅生成一张手账图片。
---

# 3D 旅行手账

把真实照片放进可以打开、翻页、旋转和查看细节的立体书。默认采用柔和的莫奈花园色彩、奶油纸张与少女风装帧；这是可调整的偏好，不是所有旅行主题的限制。

## 选择当前任务

- **修改已有网站**：先读现有源码、启动方式和用户最近反馈，直接修复对应部分，保留已有照片和交互，不重建整个项目。
- **从照片新建**：使用下面的建站流程。默认 Three.js；已有技术栈优先。
- **仅制作展示视频**：读 [视频展示](references/video-showcase.md)，演示现有网站，不为录制重做网站。默认无音乐、无旁白；画幅匹配实际构图。
- **只讨论方案**：给出具体构图建议，暂不导入照片或搭建项目。

用户最新指令优先。不重复询问已确定的来源、审美、是否配乐。餐饮、地图、输入框、照片袋不是默认必加功能。

## 建站流程

### 1. 整理照片

读取用户指定目录或附件，查看缩略图，按用户行程归类。不能确定地点的照片放入 unassigned，保留在全部照片中，不猜测地点。日期来自用户或已验证信息，不硬编码示例旅行日期。

按 [数据约定](references/data-contract.md) 创建 trip-source.json，指定稳定照片 ID。运行导入工具生成纠正 EXIF 后的照片、缩略图和尺寸数据，保留原文件：

```sh
python <skill-dir>/scripts/import_photos.py --manifest trip-source.json --out <site-dir>
```

依赖 Pillow，优先使用环境已有 Python。只有明确需重新生成这些输出时加 --overwrite。新增照片增加 ID，不让已有照片重新编号。

### 2. 设计场景跨页

每个景点分配的照片都要出现在实体页面中，不用隐藏相册代替排版。数量多时用双页或增加连续跨页。全部照片入口从清单派生，不写死数量。

读 [视觉与交互](references/design-and-interactions.md)。按数量、横竖比例设计单张大图、大小对照、横图加竖列或双页叙事。交替照片与地标位置，每页不能复制同一网格。保留原图方向和比例，不歪斜、不拉伸、不裁切人物。

按数据约定记录图片框、页边界与模型预留区域，再验证：

```sh
python <skill-dir>/scripts/validate_layout.py --trip <site-dir>/trip.json --layouts layouts.json
```

检查器核对完整性、重复、出界、重叠及侵入预留区，但不能代替实际 3D 视角检查。网页与检查器使用同一份布局数据。

### 3. 实现立体书

书封、书脊、纸页、照片框和弹起建筑处于同一 3D 场景。默认合上封面；点击封面或场景目录可打开。开合、翻页、建筑展开与相机移动协调，防止连续点击产生动画冲突。

读 [3D 实现与取景](references/three-implementation.md)。可复制 assets/framing.mjs 的透视取景函数到项目，它接收 Three.js 风格向量和四元数，不绑定库版本或导入路径。

各地点模型应可辨认，不能用同一模型换标签。需要建筑考据时查官方资料，纯代码调整无需联网。背景按需生成或采用获授权素材，不用背景替代网站功能。

### 4. 运行验收

启动本地 HTTP 服务，通过可用浏览器检查：默认合上、开合和连续翻页、各场景照片数、横竖原比例、照片查看及关闭、旋转完整取景、宽屏与窄屏。

特别检查矮视口的上下边缘。模型、照片和操作按钮不互相遮挡，旋转不频闪，也不能为防截断把书缩得过小。针对实际风险验证布局及相机投影，不为纯文案改动增加无关测试。

交付可运行网站和简短启动方式。用户需要视频时再进入视频流程。交付本地文件不代表已发布；不要主动上传私人照片、录屏或代发小红书。

## 随附材料

- scripts/import_photos.py：EXIF 纠正、原比例缩放、缩略图、去除源路径的发布清单。
- scripts/validate_layout.py：照片数据和排版几何检查，失败返回非零退出码。
- assets/framing.mjs：考虑物体深度的最小相机距离计算。
- assets/trip-source.example.json、assets/layouts.example.json：不含私人素材的结构示例。
- references/video-showcase.md：真实网站录制、静音 MP4 导出和验收。

随附材料是流程与辅助工具，不是包含照片的完整网站模板。新建时按实际旅行实现网站，修改时复用现有工程。
